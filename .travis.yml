language: python
python:
  - "2.7"
before_script: apt-get install python-numpy python-pyside python-matplotlib python-h5py && export PYTHONPATH=$PYTHONPATH:~/taskontrol

 #command to install dependencies
 #install: "pip install -r requirements.txt"
# command to run tests
script: nosetests
